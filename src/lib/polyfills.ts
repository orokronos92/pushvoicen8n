import 'regenerator-runtime/runtime'

// Importer d'autres polyfills si nécessaire pour la compatibilité navigateur
if (typeof window !== 'undefined') {
  // Polyfills spécifiques au navigateur
  if (!window.fetch) {
    // fetch polyfill si nécessaire
  }
}

export {}